import React, { Component, Link } from 'react';

const BackButton = () => {
    return (
         <Link>
            <button><i class="far fa-caret-square-left"></i></button>
        </Link>
    );
};

export default BackButton;